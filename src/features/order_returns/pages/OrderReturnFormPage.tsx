import { useEffect, useState } from "react";
import { useNavigate, useSearchParams } from "react-router-dom";
import { ApiError } from "../../../api/client";
import { BackButton } from "../../../components/BackButton";
import { FormattedNumberInput } from "../../../components/FormattedNumberInput";
import { createOrderReturn, getOrderReturn, getReturnableOrder, getReturnableOrders, updateOrderReturn } from "../services/orderReturnsApi";
import type { OrderReturn, OrderReturnFormData } from "../types/orderReturn";
import type { Order } from "../../orders/types/order";

type FormItem = { orderItemId: string; quantity: number; freeQuantity: number; description: string };
export function OrderReturnFormPage() {
  const navigate = useNavigate(); const [searchParams] = useSearchParams(); const orderId = searchParams.get("order_id"); const returnId = searchParams.get("return_id");
  const [order, setOrder] = useState<Order | null>(null); const [availableOrders, setAvailableOrders] = useState<Order[]>([]); const [selectedOrderId, setSelectedOrderId] = useState(orderId ?? ""); const [existingReturn, setExistingReturn] = useState<OrderReturn | null>(null); const [description, setDescription] = useState(""); const [returnAmount, setReturnAmount] = useState(""); const [items, setItems] = useState<FormItem[]>([]); const [isLoading, setIsLoading] = useState(true); const [isLoadingOrder, setIsLoadingOrder] = useState(false); const [isSaving, setIsSaving] = useState(false); const [error, setError] = useState<string | null>(null);
  async function loadOrder(id: string) { setIsLoadingOrder(true); setError(null); try { const loadedOrder = await getReturnableOrder(id); setOrder(loadedOrder); setSelectedOrderId(loadedOrder.id); if (loadedOrder.status !== "completed") setError("فقط سفارش تکمیل‌شده و دارای کالای قابل مرجوعی قابل ثبت مرجوعی است."); } catch (error: unknown) { setOrder(null); setError(error instanceof ApiError && error.message ? error.message : "سفارش برای ثبت مرجوعی پیدا نشد یا دیگر قابل مرجوعی نیست."); } finally { setIsLoadingOrder(false); } }
  useEffect(() => { let cancelled = false; async function load() { setIsLoading(true); setError(null); try { if (returnId) { const response = await getOrderReturn(returnId); if (cancelled) return; setExistingReturn(response); setDescription(response.description ?? ""); setReturnAmount(response.is_manual_amount ? String(response.return_amount) : ""); setItems(response.items.map((item) => ({ orderItemId: item.order_item_id ?? item.id, quantity: Number(item.quantity), freeQuantity: Number(item.free_quantity ?? 0), description: item.description ?? "" }))); if (response.order?.id) { const loadedOrder = await getReturnableOrder(response.order.id); if (!cancelled) { setOrder(loadedOrder); setSelectedOrderId(loadedOrder.id); } } } else if (orderId) { const loadedOrder = await getReturnableOrder(orderId); if (!cancelled) { setOrder(loadedOrder); setSelectedOrderId(loadedOrder.id); if (loadedOrder.status !== "completed") setError("فقط سفارش تکمیل‌شده قابل مرجوعی است."); } } else { const response = await getReturnableOrders(); if (!cancelled) { setAvailableOrders(response); if (!response.length) setError("هیچ سفارش تکمیل‌شده و دارای کالای قابل مرجوعی وجود ندارد."); } } } catch (error: unknown) { if (!cancelled) setError(error instanceof ApiError && error.message ? error.message : "اطلاعات مرجوعی قابل دریافت نیست. لطفاً وضعیت سفارش را بررسی کنید."); } finally { if (!cancelled) setIsLoading(false); } } void load(); return () => { cancelled = true; }; }, [orderId, returnId]);
  function toggleItem(orderItemId: string) { setItems((current) => current.some((item) => item.orderItemId === orderItemId) ? current.filter((item) => item.orderItemId !== orderItemId) : [...current, { orderItemId, quantity: 1, freeQuantity: 0, description: "" }]); }
  function updateItem(orderItemId: string, changes: Partial<FormItem>) { setItems((current) => current.map((item) => item.orderItemId === orderItemId ? { ...item, ...changes } : item)); }
  async function handleSubmit() { if (isSaving) return; if (!order) { setError("ابتدا یک سفارش قابل مرجوعی را انتخاب کنید."); return; } if (!items.length) { setError("حداقل یک قلم برای مرجوعی انتخاب کنید."); return; } const normalizedReturnAmount = returnAmount.trim(); if (normalizedReturnAmount) { const numericReturnAmount = Number(normalizedReturnAmount); if (!Number.isFinite(numericReturnAmount) || numericReturnAmount <= 0) { setError("مبلغ دستی مرجوعی باید بیشتر از صفر باشد."); return; } } const payload: OrderReturnFormData = { order_id: order.id, description: description.trim(), ...(normalizedReturnAmount ? { return_amount: normalizedReturnAmount } : {}), items: items.map((item) => ({ order_item_id: item.orderItemId, quantity: Math.max(1, Math.trunc(Number(item.quantity))), free_quantity: Math.max(0, Math.min(Math.trunc(Number(item.freeQuantity) || 0), Math.trunc(Number(item.quantity) || 0))), description: item.description.trim() })) }; setIsSaving(true); setError(null); try { const response = existingReturn ? await updateOrderReturn(existingReturn.id, payload) : await createOrderReturn(payload); navigate(`/order-returns/${response.id}`); } catch (error: unknown) { setError(error instanceof ApiError && error.message ? error.message : "ذخیره مرجوعی ناموفق بود."); } finally { setIsSaving(false); } }
  if (isLoading) return <section className="p-4 md:p-6"><div className="rounded-xl border border-gray-200 bg-white p-8 text-center text-sm text-gray-500">در حال دریافت اطلاعات...</div></section>;
  return <section className="space-y-6 p-4 md:p-6"><div><BackButton fallbackTo="/order-returns" label="بازگشت به مرجوعی‌ها" /><h1 className="mt-3 text-2xl font-bold text-gray-900">{existingReturn ? "ویرایش مرجوعی" : "ثبت مرجوعی جدید"}</h1>{order ? <p className="mt-1 text-sm text-gray-500">سفارش: {order.code}</p> : null}</div>
    {error ? <div role="alert" className="rounded-lg bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div> : null}
    {!existingReturn && !orderId && !order && availableOrders.length > 0 ? <div className="rounded-xl border border-gray-200 bg-white p-5"><label htmlFor="return-order" className="mb-2 block text-sm font-medium text-gray-700">سفارش تکمیل‌شده</label><select id="return-order" value={selectedOrderId} onChange={(event) => { const id = event.target.value; setSelectedOrderId(id); if (id) void loadOrder(id); }} disabled={isLoadingOrder || isSaving} className="w-full rounded-lg border border-gray-300 bg-white px-3 py-2.5 text-sm outline-none focus:border-gray-500"><option value="">انتخاب سفارش برای مرجوعی</option>{availableOrders.map((availableOrder) => <option key={availableOrder.id} value={availableOrder.id}>{availableOrder.code} — {availableOrder.customer?.customer_name ?? "مشتری نامشخص"}</option>)}</select><p className="mt-2 text-xs text-gray-500">فقط سفارش‌های تکمیل‌شده‌ای که هنوز مقداری از اقلامشان قابل مرجوعی است نمایش داده می‌شوند.</p></div> : null}
    {order ? <><div className="rounded-xl border border-gray-200 bg-white"><div className="border-b border-gray-100 px-5 py-4"><h2 className="font-semibold text-gray-900">انتخاب اقلام</h2></div><div className="overflow-x-auto"><table className="min-w-[900px] w-full text-sm"><thead className="bg-gray-50 text-right text-gray-600"><tr><th className="px-5 py-3 font-medium">انتخاب</th><th className="px-5 py-3 font-medium">محصول</th><th className="px-5 py-3 font-medium">تعداد خرید</th><th className="px-5 py-3 font-medium">رایگان</th><th className="px-5 py-3 font-medium">تعداد مرجوعی</th><th className="px-5 py-3 font-medium">توضیحات</th></tr></thead><tbody className="divide-y divide-gray-100">{order.items.map((orderItem) => { const selected = items.find((item) => item.orderItemId === orderItem.id); return <tr key={orderItem.id}><td className="px-5 py-4"><input type="checkbox" checked={Boolean(selected)} onChange={() => toggleItem(orderItem.id)} disabled={isSaving} /></td><td className="px-5 py-4"><div className="font-medium text-gray-900">{orderItem.product?.title}</div>{orderItem.product?.code ? <div className="mt-1 text-xs text-gray-500">{orderItem.product.code}</div> : null}</td><td className="px-5 py-4"><div>{orderItem.quantity}</div>{Number(orderItem.free_quantity || 0)>0?<div className="mt-1 text-xs text-emerald-600">+ {orderItem.free_quantity} رایگان</div>:null}</td><td className="px-5 py-4">{selected ? <input type="number" min={0} max={Number(orderItem.free_quantity || 0)} value={selected.freeQuantity} onChange={(event) => updateItem(orderItem.id, { freeQuantity: Number(event.target.value) })} disabled={isSaving} className="w-24 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2" /> : "—"}</td><td className="px-5 py-4">{selected ? <input type="number" min={1} max={Number(orderItem.fulfillment_quantity ?? Number(orderItem.quantity)+Number(orderItem.free_quantity||0))} value={selected.quantity} onChange={(event) => updateItem(orderItem.id, { quantity: Number(event.target.value) })} disabled={isSaving} className="w-24 rounded-lg border border-gray-300 px-3 py-2" /> : "—"}</td><td className="px-5 py-4">{selected ? <input value={selected.description} onChange={(event) => updateItem(orderItem.id, { description: event.target.value })} disabled={isSaving} placeholder="توضیح..." className="w-full rounded-lg border border-gray-300 px-3 py-2" /> : "—"}</td></tr>; })}</tbody></table></div></div><div className="rounded-xl border border-blue-100 bg-blue-50 p-5"><label className="mb-2 block text-sm font-medium text-gray-800">مبلغ مرجوعی / اعتبار مشتری</label><FormattedNumberInput dir="ltr" min="0.01" step="0.01" value={returnAmount} onValueChange={setReturnAmount} disabled={isSaving} className="w-full rounded-lg border border-gray-300 bg-white px-3 py-2 text-sm text-right" placeholder="خالی = محاسبه خودکار" /><p className="mt-2 text-xs leading-6 text-gray-600">اگر مبلغ را وارد کنید، همین مبلغ مبنای اعتبار مشتری خواهد بود؛ برای حالتی که مشتری با قیمت فعلی محصول مرجوع می‌کند. اگر خالی بماند، مبلغ طبق مبلغ فاکتور محاسبه می‌شود.</p></div><div className="rounded-xl border border-gray-200 bg-white p-5"><label htmlFor="order-return-description" className="mb-2 block text-sm font-medium text-gray-700">توضیحات مرجوعی</label><textarea id="order-return-description" value={description} onChange={(event) => setDescription(event.target.value)} rows={4} disabled={isSaving} className="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm outline-none focus:border-gray-500" placeholder="توضیحات..." /></div><div className="flex justify-end gap-3"><BackButton fallbackTo="/order-returns" label="انصراف" disabled={isSaving} /><button type="button" onClick={() => void handleSubmit()} disabled={isSaving || order.status !== "completed"} className="rounded-lg bg-gray-900 px-5 py-2 text-sm font-medium text-white hover:bg-gray-800 disabled:cursor-not-allowed disabled:opacity-50">{isSaving ? "در حال ذخیره..." : "ذخیره مرجوعی"}</button></div></> : null}
  </section>;
}

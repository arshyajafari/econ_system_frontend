import { useCallback, useEffect, useState } from "react";
import { ApiError } from "../../../api/client";
import type { Doctor } from "../../doctors/types/doctor";
import { VisitForm } from "../components/VisitForm";
import { cancelVisit, completeVisit, createVisit, getVisitDoctors, getVisits, updateVisit } from "../services/visitsApi";
import { getVisitStatusLabel, VISIT_STATUS_OPTIONS } from "../types/visit";
import type { Visit, VisitFormData, VisitListParams, VisitStatus } from "../types/visit";
function dateText(v:string|null){if(!v)return "—";const d=new Date(v);return Number.isNaN(d.getTime())?"—":new Intl.DateTimeFormat("fa-IR",{dateStyle:"short",timeStyle:"short"}).format(d)}
export function VisitsPage(){const[visits,setVisits]=useState<Visit[]>([]),[doctors,setDoctors]=useState<Doctor[]>([]),[doctorId,setDoctorId]=useState(""),[status,setStatus]=useState<VisitStatus|"">(""),[from,setFrom]=useState(""),[to,setTo]=useState(""),[search,setSearch]=useState(""),[page,setPage]=useState(1),[lastPage,setLastPage]=useState(1),[total,setTotal]=useState(0),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[actionId,setActionId]=useState<string|null>(null),[error,setError]=useState<string|null>(null),[formError,setFormError]=useState<string|null>(null),[formOpen,setFormOpen]=useState(false),[editing,setEditing]=useState<Visit|null>(null);
 const load=useCallback(async(p?:Partial<VisitListParams>)=>{setLoading(true);setError(null);try{const r=await getVisits({search:search.trim()||undefined,doctor_id:doctorId||undefined,status:status||undefined,visit_from:from||undefined,visit_to:to||undefined,sort:"-visit_date",page:p?.page??page,per_page:20});setVisits(r.data);setLastPage(r.meta.last_page);setTotal(r.meta.total)}catch(e:unknown){setError(e instanceof ApiError?e.message:"خطا در دریافت بازدیدها.")}finally{setLoading(false)}},[doctorId,from,page,search,status,to]);
 useEffect(()=>{let cancelled=false;const run=async()=>{setLoading(true);setError(null);try{const r=await getVisits({search:search.trim()||undefined,doctor_id:doctorId||undefined,status:status||undefined,visit_from:from||undefined,visit_to:to||undefined,sort:"-visit_date",page,per_page:20});if(!cancelled){setVisits(r.data);setLastPage(r.meta.last_page);setTotal(r.meta.total)}}catch(e:unknown){if(!cancelled)setError(e instanceof ApiError?e.message:"خطا در دریافت بازدیدها.")}finally{if(!cancelled)setLoading(false)}};void run();return()=>{cancelled=true}},[doctorId,from,page,search,status,to]);
 useEffect(()=>{getVisitDoctors().then(setDoctors).catch(()=>setDoctors([]))},[]);
 const reset=()=>{setSearch("");setDoctorId("");setStatus("");setFrom("");setTo("");setPage(1)};
 async function submit(d:VisitFormData){setSaving(true);setFormError(null);try{const saved=editing?await updateVisit(editing.id,d):await createVisit(d);setVisits(x=>editing?x.map(v=>v.id===saved.id?saved:v):[saved,...x].slice(0,20));if(!editing)setTotal(x=>x+1);setFormOpen(false);setEditing(null)}catch(e:unknown){setFormError(e instanceof ApiError?e.message:"خطا در ذخیره بازدید.")}finally{setSaving(false)}}
 async function action(v:Visit,a:"complete"|"cancel"){if(actionId||!window.confirm(a==="complete"?"بازدید تکمیل شود؟":"بازدید لغو شود؟"))return;setActionId(v.id);try{const u=a==="complete"?await completeVisit(v.id):await cancelVisit(v.id);setVisits(x=>x.map(i=>i.id===u.id?u:i))}catch(e:unknown){setError(e instanceof ApiError?e.message:"خطا در تغییر وضعیت بازدید")}finally{setActionId(null)}}
 return <section className="space-y-5 p-4 md:p-6"><header className="flex flex-col gap-3 md:flex-row md:justify-between"><div><h1 className="text-2xl font-bold">بازدیدها</h1><p className="text-sm text-gray-500">ثبت و پیگیری بازدیدهای پزشکان</p></div><div className="flex gap-2"><button type="button" onClick={()=>void load()} disabled={loading} className="rounded-lg border px-4 py-2">بروزرسانی</button><button type="button" onClick={()=>{setEditing(null);setFormError(null);setFormOpen(true)}} className="rounded-lg bg-gray-900 px-4 py-2 text-white">بازدید جدید</button></div></header>{error&&<div role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</div>}{formOpen&&<VisitForm doctors={doctors} visit={editing} isSubmitting={saving} error={formError} onSubmit={submit} onCancel={()=>{if(!saving){setFormOpen(false);setEditing(null)}}}/>}<div className="grid gap-3 md:grid-cols-2 lg:grid-cols-6"><input value={search} onChange={e=>{setSearch(e.target.value);setPage(1)}} placeholder="جستجو" className="rounded-lg border px-3 py-2 lg:col-span-2"/><select value={doctorId} onChange={e=>{setDoctorId(e.target.value);setPage(1)}} className="rounded-lg border px-3 py-2"><option value="">همه پزشکان</option>{doctors.map(d=><option key={d.id} value={d.id}>{d.first_name} {d.last_name}</option>)}</select><select value={status} onChange={e=>{setStatus(e.target.value as VisitStatus|"");setPage(1)}} className="rounded-lg border px-3 py-2"><option value="">همه وضعیت‌ها</option>{VISIT_STATUS_OPTIONS.map(s=><option key={s.value} value={s.value}>{s.label}</option>)}</select><input type="date" value={from} onChange={e=>{setFrom(e.target.value);setPage(1)}} className="rounded-lg border px-3 py-2"/><input type="date" value={to} onChange={e=>{setTo(e.target.value);setPage(1)}} className="rounded-lg border px-3 py-2"/></div><div className="flex justify-between text-sm text-gray-500"><span>{new Intl.NumberFormat("fa-IR").format(total)} بازدید</span><button type="button" onClick={reset}>پاک کردن فیلترها</button></div><div className="overflow-x-auto rounded-xl border bg-white"><table className="min-w-full text-right text-sm"><thead className="bg-gray-50"><tr>{["تاریخ","پزشک","هدف","کارمند","وضعیت","عملیات"].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{loading&&!visits.length?<tr><td colSpan={6} className="p-10 text-center">در حال دریافت…</td></tr>:!visits.length?<tr><td colSpan={6} className="p-10 text-center">بازدیدی پیدا نشد.</td></tr>:visits.map(v=><tr key={v.id}><td className="px-4 py-3 whitespace-nowrap">{dateText(v.visit_date)}</td><td className="px-4 py-3">{v.doctor?.name??"—"}</td><td className="px-4 py-3">{v.purpose||"—"}</td><td className="px-4 py-3">{v.employee?.name??"—"}</td><td className="px-4 py-3">{getVisitStatusLabel(v.status)}</td><td className="px-4 py-3">{v.status==="draft"?<div className="flex gap-2"><button type="button" onClick={()=>{setEditing(v);setFormError(null);setFormOpen(true)}} className="rounded border px-2 py-1 text-xs">ویرایش</button><button type="button" disabled={actionId===v.id} onClick={()=>void action(v,"complete")} className="rounded border px-2 py-1 text-xs">تکمیل</button><button type="button" disabled={actionId===v.id} onClick={()=>void action(v,"cancel")} className="rounded border px-2 py-1 text-xs">لغو</button></div>:"—"}</td></tr>)}</tbody></table></div>{lastPage>1&&<div className="flex justify-center gap-3"><button type="button" disabled={page<=1||loading} onClick={()=>setPage(x=>x-1)} className="rounded border px-4 py-2">قبلی</button><span className="py-2">صفحه {page} از {lastPage}</span><button type="button" disabled={page>=lastPage||loading} onClick={()=>setPage(x=>x+1)} className="rounded border px-4 py-2">بعدی</button></div>}</section>}
